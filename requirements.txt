gradio>=6.0.0
openai>=1.30.0
arxiv>=2.1.0
requests>=2.31.0
tenacity>=8.2.3
PyPDF2>=3.0.0
python-dotenv>=1.0.0

# v2.0 搜索功能依赖
jieba>=0.42.0

# NumPy版本限制（避免与matplotlib/pandas兼容性问题）
numpy>=1.24.0,<2.0.0

# v2.1 RAG语义搜索
sentence-transformers>=2.2.0

# ModelScope模型下载（创空间使用）
modelscope>=1.10.0

# 高级PDF解析（Docling 可能触发 PyYAML 冲突，先注释；MinerU+doclayout-yolo 成功后可去掉注释）
# docling>=2.0.0

# MinerU高精度解析（不用 mineru[all]，避免 PyYAML 冲突）
mineru>=2.0.0
# MinerU 布局模型依赖（单独安装避免拉入 vllm/xformers 等重依赖）
doclayout-yolo

# 向量数据库
faiss-cpu>=1.7.4

# 关键词检索
rank-bm25>=0.2.2

# OCR截图识别（可选）
# paddleocr>=2.7.0  # 注意：可能与系统PyYAML冲突
# easyocr>=1.7.0  # 备选方案

# 中文分词模型 (可选)
# spacy>=3.7.0
