gradio>=6.0.0
openai>=1.30.0
arxiv>=2.1.0
requests>=2.31.0
tenacity>=8.2.3
PyPDF2>=3.0.0
python-dotenv>=1.0.0

# v2.0 搜索功能依赖
jieba>=0.42.0

# NumPy版本限制（避免与matplotlib/pandas兼容性问题）
numpy>=1.24.0,<2.0.0

# v2.1 RAG语义搜索
sentence-transformers>=2.2.0

# 高级PDF解析
docling>=2.0.0

# MinerU高精度解析（可选，约3GB模型）
# pip install mineru[all]>=2.7.0
# 需要额外配置 magic-pdf.json，详见 docs/MODEL_STORAGE_CONFIG.md

# 向量数据库
faiss-cpu>=1.7.4

# 关键词检索
rank-bm25>=0.2.2

# OCR截图识别（可选，二选一）
paddleocr>=2.7.0
# easyocr>=1.7.0  # 备选方案

# 中文分词模型 (可选)
# spacy>=3.7.0
